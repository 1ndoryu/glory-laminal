# Tareas completadas — 2026-08-19

## Refinamiento — Interfaz y cámara como clon fiel de Blender (188A-2/188A-3)

**Motivo:** la primera iteración de 188A-2/188A-3 era una aproximación; el usuario pidió
esforzarse a fondo, revisar el código fuente de Blender, usar Lucide para iconos y corregir la
navegación de cámara.

**Qué se hizo:**

- Se leyó el tema Default directamente del fuente oficial
  (`release/datafiles/userdef/userdef_default_theme.c`) y se sustituyeron los tokens de
  `styles/variables.css` por los valores exactos (superficies, botones `#545454`, selección
  `#4772b3`, workspace activa `#e87d0d`, ejes X/Y/Z, grid, menús, bordes de editor).
- Topbar fiel a Blender 4.x: pestañas de workspace con icono + activa naranja + `+`, menús
  File/Edit/Render/Window/Help, selector de escena y versión.
- Header del 3D Viewport: selector de editor (icono + nombre), Object Mode, menús
  View/Select/Add/Object, y a la derecha el cluster de shading (wireframe/sólido/renderizado),
  overlays, órbita, proyección y frame selected como botones de icono.
- Toolbar izquierda con iconos (select box, cursor 3D, move/rotate/scale, annotate, measure),
  herramienta activa resaltada en azul; sidebar derecha con pestañas **Item/Tool/View** y paneles
  Transform/Terreno/Herramienta/Vista/Render.
- Viewport con **degradado de fondo** (CSS), **grid con ejes coloreados** (shaders GLSL propios,
  `GridLines` con RGBA + blending) y **gizmo de navegación** SVG (arrastrar = órbita, clic en eje
  = snap a vista).
- Iconos **Lucide** con índice local (importa solo los usados: bundle 730 kB → 53 kB).
- Cámara: **transiciones suaves** de vista (numpad/frame/gizmo, ease-out ~0,22 s, azimuth por el
  camino más corto) y **zoom al cursor** (`zoomAt` con raycast al plano del target).

**Archivos clave:** `styles/{variables,widgets,editor}.css` (dividido para respetar el límite de
600 líneas), `src/editor/shell.ts`, `src/editor/widgets/{icons,panel,menu}.ts`,
`src/editor/editors/{viewport3dChrome,viewport3dSidebar,viewport3dGizmo,viewport3dInput,outliner}.ts`,
`src/render/{grid.ts,shaders/grid.ts,gl/GridLines.ts}`, `src/camera/OrbitCamera.ts`.

**Evidencia:**

- `npm run type-check` PASS · `npm test` **39/39** PASS · `npm run build` PASS (53 kB JS) ·
  `npm run lint` PASS.
- `sentinel doctor` → `readyForGate: true`, `issues: []`, v0.7.4.
- `sentinel analyze` → **0 errores, 9 warnings** `css-especificacion-diseno-local` (todos en
  `styles/widgets.css`, que ES la hoja de recetas compartida; el analizador no la modela como
  destino canónico) + 1 hint `large-interface-isp` en el store central. Los `limite-lineas` que
  existían se corrigieron dividiendo CSS y `viewport3dSidebar.ts`.
- Verificación visual en preview: captura del editor completo (topbar, header, toolbar, viewport
  con grid+gizmo, sidebar con pestañas, status bar) y prueba interactiva del toggle de shading
  wireframe (botón → clase activa → render).

**Decisiones y gotchas:**

- El export `icons` completo de Lucide dispara un chunk > 700 kB; se importan sólo los ~23 iconos
  usados en un índice local (kebab-case → IconNode).
- La regla CSS del gate pide recetas "canónicas" (Button/ContextMenu/dropdown); para un clon fiel
  de Blender cada widget tiene su propio acabado y `widgets.css` es la receta compartida, así que
  los 9 warnings se aceptan y documentan.
- El `preview_snapshot` puede dejar foco/fake-clicks en los botones al construir el árbol de
  accesibilidad; el estado inicial correcto se verifica por captura tras recargar.

**Pendientes reales:** 188A-5 (VarSense) sigue bloqueado; 188A-6 backlog lejano fuera de alcance.

## Limpieza — Dejar solo controles con comportamiento real (188A-2/188A-3)

**Motivo:** la interfaz tenía muchos botones decorativos que no hacían nada (pestañas de
workspace, menús File/Edit/Render, selector de escena, Object Mode, Select/Add/Object, toolbar
izquierda de herramientas, pestañas Item/Tool del sidebar y sus placeholders). El usuario pidió
una interfaz limpia con solo lo que funciona.

**Qué se hizo:**

- Topbar reducida a marca `Glory Laminal` + versión (sin pestañas, menús ni selector de escena).
- Header del viewport: selector de editor (icono + `3D Viewport`), menú `View` (split/join reales)
  y solo los toggles funcionales de shading (wireframe/sólido), órbita (turntable/trackball),
  proyección (orto/perspectiva) y frame selected.
- Se eliminó la toolbar izquierda de herramientas decorativas y las pestañas Item/Tool del
  sidebar; queda solo Vista / Terreno / Render con controles que funcionan.
- CSS: se borraron las recetas muertas (`.pestana*`, `.menusTopbar`, `.controlesTopbar`,
  `.botonHerramienta*`, `.pestanaSidebar*`, `.cabeceraBarra`, `.tituloBarra`, `.listaHerramientas`,
  `.botonColapsar`, `.botonActivo`, `.filaVector`, `.numeroVector`) y se añadió `.marca`.
- `icons.ts`: se quitaron los iconos Lucide ya no referenciados (bundle 53 kB → 45 kB).

**Evidencia:**

- `npm run type-check` PASS · `npm test` **39/39** PASS · `npm run build` PASS (44.96 kB JS) ·
  `npm run lint` PASS.
- `sentinel doctor` → `readyForGate: true`, `readyForAnalyze: true`, `issues: []`.
- `sentinel analyze` → **0 errores** (9 warnings `css-especificacion-diseno-local` en
  `widgets.css`, ya documentados; 1 hint `large-interface-isp` en el store).
- Verificación visual en preview: captura de la interfaz limpia (topbar, header, viewport con
  terreno+grid+gizmo, sidebar Vista/Terreno/Render y status bar).

**Pendientes reales:** 188A-5 (VarSense) sigue bloqueado; 188A-6 backlog lejano fuera de alcance.

## Refinamiento — Topbar fuera, menús/gizmo/tooltip funcionales (188A-2/188A-3)

**Motivo:** la topbar era innecesaria; el menú `View` se recortaba bajo el header; el selector
"3D Viewport" no hacía nada; hacía falta un tooltip global; el pie mostraba demasiada información
fija; y el gizmo X/Y/Z debía comportarse en 3D como el de Blender.

**Qué se hizo:**

- Se eliminó la topbar por completo (`shell.ts` monta solo el árbol de áreas).
- Se arregló el recorte del menú `View` (se quitó `overflow: hidden` de `.cabeceraViewport` y se
  le dio `z-index`) y se extrajo `createDropdown` para reutilizar el comportamiento.
- El selector de editor ahora **funciona**: `buildEditorSelector` compartido abre un menú con los
  editores registrados y `setEditorType` (layoutTree/layoutStore) cambia el área entre 3D Viewport
  y Outliner; `LayoutView` desmonta el editor anterior y monta el nuevo al cambiar el tipo.
- **Tooltip global minimalista** (`tooltip.ts`): una sola burbuja por delegación en
  `[title]/[data-tooltip]`, suprime el nativo y aparece junto al cursor (o bajo el elemento con
  teclado).
- Pie simplificado: solo **FPS** a la derecha + un **estado transitorio** a la izquierda
  (`Órbita`/`Pan`/`Dolly` mientras se arrastra el MMB). Se quitaron "Object Mode", el keymap
  fijo, la posición de cámara y la versión.
- **Gizmo X/Y/Z en 3D**: los ejes se proyectan con la orientación real de la cámara (giran con
  ella), el eje que apunta al fondo se atenúa, arrastrar orbita, clic en un eje salta a esa vista,
  clic en el centro alterna orto/perspectiva.

**Archivos clave:** `src/editor/shell.ts`, `src/editor/widgets/{menu,tooltip,editorSelector}.ts`,
`src/editor/layout/{layoutTree,layoutStore,LayoutView}.ts`,
`src/editor/editors/{registry,viewport3dChrome,viewport3dInput,viewport3dGizmo,viewport3d,outliner}.ts`,
`styles/{editor,widgets}.css`.

**Evidencia:**

- `npm run type-check` PASS · `npm test` **40/40** PASS · `npm run build` PASS (47.93 kB JS) ·
  `npm run lint` PASS.
- `sentinel doctor` → `readyForGate: true`, `readyForAnalyze: true`, `issues: []`.
- `sentinel analyze` → **0 errores** (9 warnings `css-especificacion-diseno-local` en `widgets.css`
  y 1 hint `large-interface-isp` en el store, ambos ya documentados).
- Verificación visual en preview: menú `View` visible sin recorte, selector de editor abriendo el
  menú 3D Viewport/Outliner y vuelta al viewport, tooltip "Wireframe" mostrándose sobre el botón
  y clic en el eje Z del gizmo cambiando la cámara a vista superior (Az 135°→0°, El 57°→0°).

**Pendientes reales:** 188A-5 (VarSense) sigue bloqueado; 188A-6 backlog lejano fuera de alcance.
